#ifndef DOOR_STRUCT_H
#define DOOR_STRUCT_H

struct door {
    int id;
    int status;
};

#endif

// typedef struct door_struct {
//     int id;
//     int status;
//     Struct node* cur = smth;
// } door; // can write like this so we don't have to use stucrt word every time
// only in declaration and inside it
